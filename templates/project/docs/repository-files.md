# Managed repository files

This inventory lists the actual composed package paths. Exact ownership and
upgrade strategies are recorded in `_starter-kit-files.json`; source-only tests,
manufacturing commands and maintenance history are excluded. Files outside this
inventory belong to the application, regardless of their directory name.
